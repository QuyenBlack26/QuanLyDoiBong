<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trangchitietdoibong"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            android:name=".LoginActivity"
            android:exported="false"
            android:label="Đăng Nhập" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Đăng Ký" />

        <activity
            android:name=".HomeNewActivity"
            android:exported="true"
            android:label="Trang Chủ Mới">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ChiTietCauThuActivity" android:exported="false"/>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="Trang Chủ" />

        <activity
            android:name=".NewsDetailActivity"
            android:exported="false"
            android:label="Chi tiết tin tức" />

        <activity
            android:name=".AddNewsActivity"
            android:exported="false"
            android:label="Thêm tin tức" />

        <activity
            android:name=".activity.PlayerManagementActivity"
            android:exported="false"
            android:label="Quản lý cầu thủ" />

    </application>

</manifest>